# System Documentation

Document implemented behavior here after a system becomes stable enough to explain. Use one file per domain or system, such as `kernel.md` or `spawning.md`.

Describe responsibilities, owned state, public interfaces, tick flow, and important constraints. Keep these documents synchronized with the current code; historical reasoning belongs in `decisions/` and chronological progress belongs in `rewrite-log/`.
